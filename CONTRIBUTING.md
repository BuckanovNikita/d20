# Руководство для разработчиков

Спасибо за интерес к разработке d20! Этот документ поможет вам начать работу над проектом.

## 🚀 Начало работы

### Установка

```bash
git clone <repository-url>
cd d20
uv sync --extra dev
```

### Структура проекта

```
d20/
├── src/d20/          # Исходный код библиотеки
│   ├── cli.py        # CLI интерфейс
│   ├── convert.py    # Основная логика конвертации
│   ├── formats/      # Конвертеры форматов
│   │   ├── base.py   # Базовый класс конвертера
│   │   ├── coco.py   # COCO конвертер
│   │   ├── yolo.py   # YOLO конвертер
│   │   └── voc.py    # PASCAL VOC конвертер
│   └── types.py      # Типы данных
├── tests/            # Тесты
└── pyproject.toml    # Конфигурация проекта
```

## 🏗️ Архитектура проекта

Проект использует классовую архитектуру для конвертеров форматов:

- **`FormatConverter`** - базовый абстрактный класс в `formats/base.py`
- **Конкретные конвертеры** - `CocoConverter`, `YoloConverter`, `VocConverter`
- Каждый конвертер реализует методы `autodetect()`, `read()`, `write()`

### Добавление нового формата

1. Создайте новый класс конвертера в `src/d20/formats/`
2. Наследуйте от `FormatConverter`
3. Реализуйте методы `autodetect()`, `read()`, `write()`
4. Зарегистрируйте конвертер в `formats/__init__.py`
5. Добавьте тесты в `tests/test_conversions.py`

### Основные типы данных

- `ImageInfo` - информация об изображении
- `Annotation` - аннотация объекта
- `Split` - сплит датасета (train/val/test)
- `Dataset` - универсальный формат датасета

## 🧪 Тестирование

Проект требует минимум 80% покрытия кода. Тестовые датасеты находятся в `tests/fixtures/datasets/`.

HTML-отчеты FiftyOne сохраняются в `tests/reports/<dataset>/<step>/index.html` после выполнения тестов конвертации.

## 📚 Документация

Все публичные функции и классы должны иметь docstrings в формате Google style:

```python
def convert_dataset(request: ConversionRequest) -> None:
    """Конвертирует датасет из одного формата в другой.

    Args:
        request: Запрос на конвертацию с указанием форматов и путей

    Raises:
        ValueError: Если формат не поддерживается или данные некорректны
    """
    ...
```

README.md предназначен для пользователей, не для разработчиков.

## 🤝 Процесс внесения изменений

1. Создайте ветку от `main`:
   ```bash
   git checkout -b feature/my-feature
   ```

2. Внесите изменения и убедитесь, что:
   - Все тесты проходят
   - Код отформатирован (`ruff format`)
   - Нет ошибок линтера (`ruff check`)
   - Нет ошибок типов (`mypy`)

3. Создайте Pull Request с описанием изменений

4. Дождитесь ревью и внесите необходимые правки

### Сообщения коммитов

Используйте понятные сообщения коммитов:

```
feat: добавлена поддержка формата CustomFormat
fix: исправлена обработка путей в Windows
docs: обновлена документация по API
test: добавлены тесты для YOLO конвертера
refactor: рефакторинг базового класса конвертера
```

## 🐛 Отчеты об ошибках

При создании issue об ошибке укажите:

1. Описание проблемы - что происходит
2. Шаги для воспроизведения - как воспроизвести ошибку
3. Ожидаемое поведение - что должно происходить
4. Фактическое поведение - что происходит на самом деле
5. Версия Python и d20
6. Логи ошибок (если есть)

## 💡 Предложения новых функций

При предложении новой функции:

1. Создайте issue с описанием функции
2. Объясните, зачем она нужна
3. Предложите возможную реализацию (если есть идеи)
4. Обсудите с мейнтейнерами перед началом работы

## 🎯 Приоритеты разработки

Текущие приоритеты:

1. Поддержка новых форматов датасетов
2. Улучшение производительности для больших датасетов
3. Расширение возможностей визуализации
4. Улучшение обработки ошибок и сообщений пользователю

## 📞 Вопросы

Если у вас есть вопросы:

- Создайте issue в репозитории
- Обсудите в Pull Request
- Свяжитесь с мейнтейнерами проекта

---

Спасибо за вклад в d20! 🎉
